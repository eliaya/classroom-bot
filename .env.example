# Web UI settings
# The current Vite app is a static dashboard/simulator and does not require runtime secrets.
APP_URL="http://localhost:5173"

# Optional: only needed if future web features call Gemini APIs.
GEMINI_API_KEY=""
